// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove experimental.serverActions — it’s on by default in Next 14+
  // Other config can stay here as needed
};

export default nextConfig;
